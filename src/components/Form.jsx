/** @jsx jsx */
import React from "react";
import { css, jsx } from "@emotion/core";

const Form = props => (
  <form css={{ textAlign: "center", width: "100%" }} {...props} />
);

export default Form;
